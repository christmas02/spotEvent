import { passwordRules, emailRules, phoneCiRules, requiredRules  } from "@/common/rules";


export default {
    data() {
        return {
           passwordRules,
            requiredRules,
            phoneCiRules,
            emailRules
        }
    },
};
